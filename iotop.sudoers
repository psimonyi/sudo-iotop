# Allow users in the wheel and iotop groups to run iotop with no password.
# Note that iotop essentially lets you ionice too.

%wheel ALL = NOPASSWD: /usr/sbin/iotop
%iotop ALL = NOPASSWD: /usr/sbin/iotop

